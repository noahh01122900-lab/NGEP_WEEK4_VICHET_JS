// Exercise 1: Selecting DOM Elements
// Open index.html in the browser and check the console (F12) as you work.

// TODO 1: Use document.getElementById() to select the <h1> (id="main-title").
// Log the element to the console.


// TODO 2: Use document.querySelector() to select the FIRST element with class "fruit".
// Log its text (use .textContent, covered fully in the next exercise, or just log
// the element itself for now).


// TODO 3: Use document.querySelectorAll() to select ALL elements with class "fruit".
// Log how many were found using .length.


// TODO 4: querySelectorAll() returns a NodeList, not an array — but it does support
// forEach(). Loop over the fruit list and console.log() each fruit element.


// TODO 5: Use querySelector() with an ID selector ("#my-button") instead of
// getElementById(), to select the button. Log it.
// (Both approaches work for IDs — querySelector is more flexible for CSS selectors.)
